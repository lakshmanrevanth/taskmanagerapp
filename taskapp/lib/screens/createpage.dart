import 'package:flutter/material.dart';

class CreateForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build

    return Scaffold(
      appBar: AppBar(
        title: Text("Create Page"),
      ),
      body: Center(
        child: Text("hello world from create page"),
      ),
    );
  }
}
